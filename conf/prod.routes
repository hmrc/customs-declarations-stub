# Add all the application routes to the app.routes file
->         /                          health.Routes

POST        /                       controllers.SubmissionsController.submit()
POST        /cancellation-requests  controllers.SubmissionsController.cancel()